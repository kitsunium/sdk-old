name: CI

on:
  push:
    branches: [main]
    tags: ["*"]
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0 # Needed for SonarCloud

      - uses: actions/setup-go@v5
        with:
          go-version: 1.24.6
          cache: true

      - uses: bazel-contrib/setup-bazel@0.15.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true

      - uses: golangci/golangci-lint-action@v8
        with:
          version: v2.4.0
          skip-cache: true

      - name: Validate
        run: |
          go mod verify
          go fmt ./...
          git diff --exit-code || (echo "❌ Files need formatting" && exit 1)

      - name: Test
        run: bazel test //pkg/... --test_output=errors

      - name: Coverage
        if: github.event_name == 'pull_request'
        run: |
          echo "📊 Generating coverage with Bazel..."
          # Generate coverage with Bazel
          bazel coverage --combined_report=lcov //pkg/... || true

          # Find and convert the coverage report
          if [ -f bazel-out/_coverage/_coverage_report.dat ]; then
            echo "✅ Coverage report found, converting for SonarCloud..."
            # Copy the LCOV file to the expected location
            cp bazel-out/_coverage/_coverage_report.dat coverage.lcov
            
            # Convert absolute paths to relative for SonarCloud
            sed -i.bak 's|SF:.*/sdk/|SF:|g' coverage.lcov
            
            echo "📈 Coverage report ready, but disabled in sonar-project.properties"
          else
            echo "⚠️ No coverage report generated, creating empty file..."
            touch coverage.lcov
          fi

      - name: SonarCloud Scan
        if: github.event_name == 'pull_request'
        uses: SonarSource/sonarqube-scan-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=${{ secrets.ORGANIZATION_NAME }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.pullrequest.key=${{ github.event.pull_request.number }}
            -Dsonar.pullrequest.branch=${{ github.head_ref }}
            -Dsonar.pullrequest.base=${{ github.base_ref }}
        continue-on-error: true

      - name: Security Scan
        uses: aquasecurity/trivy-action@0.32.0
        if: always()
        with:
          scan-type: fs
          format: sarif
          output: trivy.sarif
          severity: CRITICAL,HIGH
        continue-on-error: true

      - name: Upload Security Results
        uses: github/codeql-action/upload-sarif@v3
        if: always() && hashFiles('trivy.sarif') != ''
        with:
          sarif_file: trivy.sarif
